{
  "properties": {
    "detail-type": {
      "type": "string",
      "enum": [
        "Account subscribed"
      ],
      "description": "The type of the event."
    },
    "detail": {
      "$ref": "./AccountSubscribedEventDetail.schema.json"
    },
    "version": {
      "type": "string",
      "enum": [
        "0"
      ],
      "description": "Version of the event schema."
    },
    "id": {
      "format": "uuid",
      "type": "string",
      "description": "A unique id identifying the event. You can use this id to deal with message duplicity."
    },
    "source": {
      "type": "string",
      "enum": [
        "componently.vendor"
      ],
      "description": "An identifier you can use to separate componently events from other events."
    },
    "time": {
      "pattern": "d{4}-[01]d-[0-3]dT[0-2]d:[0-5]d:[0-5]d.d+Z?",
      "type": "string",
      "description": "When the event happened."
    }
  },
  "type": "object",
  "required": [
    "detail-type",
    "detail",
    "version",
    "id",
    "source",
    "time"
  ],
  "title": "AccountSubscribedEvent",
  "description": "This event is sent when a user has bought a paid subscription.",
  "example": {
    "version": "0",
    "id": "3844124c-cb00-41c7-8803-cefc1d4d9b34",
    "detail-type": "Account subscribed",
    "source": "componently.vendor",
    "time": "2020-06-01T18:19:00Z",
    "detail": {
      "userId": "05eed33f-1469-4e93-ba9c-78a622b091b4",
      "subscriptionId": "731065ff-e134-4d12-b9c6-6cad02b3d79b"
    }
  }
}